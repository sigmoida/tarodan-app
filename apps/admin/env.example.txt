# =============================================================================
# TARODAN ADMIN PANEL - ENVIRONMENT CONFIGURATION
# RENAME THIS FILE TO: .env.local
# Sayfalar ve E-posta Şablonları: API'nin ayakta olması (NEXT_PUBLIC_API_URL) ve
# admin girişi yapılmış olması gerekir. API çalışmıyorsa sayfa listesi yüklenmez.
# =============================================================================

# API Configuration (API sunucusu bu adreste çalışmalı; varsayılan: npm run dev veya start in apps/api)
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_API_PREFIX=/api

# App Configuration
NEXT_PUBLIC_APP_NAME=Tarodan Admin
NEXT_PUBLIC_APP_URL=http://localhost:3002

# =============================================================================
# TARODAN ADMIN - ENVIRONMENT CONFIGURATION
# `pnpm dev` bu dosyayı otomatik olarak .env.local'e kopyalar (elle de kopyalayabilirsin).
# =============================================================================

# API Configuration (API sunucusu bu adreste çalışmalı)
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_API_PREFIX=/api

# App Configuration
NEXT_PUBLIC_APP_NAME=Tarodan Admin
NEXT_PUBLIC_APP_URL=http://localhost:3002

# Sentry (opsiyonel; withSentryConfig sadece DSN + SENTRY_AUTH_TOKEN varsa aktif)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=tarodan
SENTRY_PROJECT=admin

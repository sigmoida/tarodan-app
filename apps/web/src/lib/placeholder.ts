/** @format */

/**
 * `imagePlaceholder` gövdesi `@tarodan/shared`'a taşındı: ilan formunun görsel
 * ızgarası paketin içine girdi ve paket `apps/web`ten import edemez.
 *
 * Bu dosya eski yolu ayakta tutar — mevcut çağrı yerleri değişmesin diye.
 */
export { imagePlaceholder } from "@tarodan/shared";

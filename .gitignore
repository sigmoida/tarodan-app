# Dependencies
**/node_modules/
.pnpm-store/

# Build outputs
dist/
dist-seed/
.next/
# iCloud sync bozuk ' 2' kopyalı eski .next dizinleri kenara alındı (Disk Utility First Aid sonrası silinmeli)
_corrupt_next_*
.turbo/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
**/package-lock.json

# Cache
.cache/
.eslintcache

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local ngrok binary (copy from ngrok.com; do not commit)
tools/ngrok

# Testing
coverage/

# Prisma
prisma/*.db
prisma/*.db-journal
node_modules

# Local / one-off: image generation and MVP docs (not needed in repo)
docs/IMAGE_GENERATION_PROMPTS.md
docs/MVP_IMAGE_PLAN.md
scripts/generate-images.mjs


# @tarodan/brand tarafından üretilen marka logoları (sync:brand / predev / prebuild)
apps/web/public/tarodan-logo.jpg
apps/web/public/tarodan-logo-transparent.png
apps/admin/public/tarodan-logo.jpg
apps/admin/public/tarodan-logo-transparent.png
apps/web/public/tarodan-favicon.png
apps/admin/public/tarodan-favicon.png
# Build/dev sırasında generator tarafından üretilen toplu ürün şablonu
apps/admin/public/templates/tarodan-toplu-urun-sablonu.xlsx
.claude/

# PayTR Direkt API vendor PDF/örnek klasörü — lokal referans, repoya girmez
PayTR Direkt API/
# PayTR iFrame API vendor PDF/örnek klasörü — lokal referans, repoya girmez
PayTR IFrame API/
# Playwright MCP test artefaktları (snapshot/screenshot/log) — repoya girmez
.playwright-mcp/

# Cursor IDE ayar/plan dosyaları — repoya girmez (içerik junk/cursor-plans altına taşındı)
.cursor/

# Claude Code skills lockfile (local)
skills-lock.json

# Conductor (local workspace settings) — repoya girmez
.conductor/

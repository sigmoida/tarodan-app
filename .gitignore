# Dependencies
**/node_modules/
.pnpm-store/

# Build outputs
dist/
dist-seed/
.next/
# iCloud sync bozuk ' 2' kopyalı eski .next dizinleri kenara alındı (Disk Utility First Aid sonrası silinmeli)
_corrupt_next_*
.turbo/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Cache
.cache/
.eslintcache

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local ngrok binary (copy from ngrok.com; do not commit)
tools/ngrok

# Testing
coverage/

# Prisma
prisma/*.db
prisma/*.db-journal
node_modules

# Local / one-off: image generation and MVP docs (not needed in repo)
docs/IMAGE_GENERATION_PROMPTS.md
docs/MVP_IMAGE_PLAN.md
scripts/generate-images.mjs

# Seed tarafından photos/ kopyalanır; kaynak photos/ root'ta
apps/web/public/photos/
.claude/

# PayTR Direkt API vendor PDF/örnek klasörü — lokal referans, repoya girmez
PayTR Direkt API/
# PayTR iFrame API vendor PDF/örnek klasörü — lokal referans, repoya girmez
PayTR IFrame API/
# Playwright MCP test artefaktları (snapshot/screenshot/log) — repoya girmez
.playwright-mcp/

{
  "name": "@tarodan/shared",
  "version": "1.0.0",
  "description": "Platform-agnostic domain metadata (status/enum → label + semantic variant) shared by @tarodan/ui (web) and @tarodan/ui-native (mobile).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./src/*": "./src/*.ts"
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
